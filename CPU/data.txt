metodo         = esparso;        // esparso (sparse routines) or denso (dense routines)
openmp         = OFF;            // enable/disable OpenMP Parallelism
v_inicial      = 1.0;            // flat start
theta_inicial  = 0.0;
tolerancia     = 0.0001;
no_max_iter    = 100;
arq_entrada    = ieee118.cdf;     // input file name
laconic_mode   = ON;             // if turned on doesn't print on the terminal and generates benchmark csv file
output_bench   = SCREEN;         // SCREEN, FILE ou ALL
output_ans     = OFF;